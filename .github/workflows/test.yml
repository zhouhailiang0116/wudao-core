name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run axiom tests
        run: |
          # Mock wukong/memory directory (expected by axiom8_causal.py path layout)
          mkdir -p wukong/memory
          python3 axioms/tests/test_axioms.py 2>&1
